# Core dependencies (cross-platform)
lxml>=6.1,<7
pymupdf>=1.28,<2
python-docx>=1.2,<3
typing-extensions>=4.16.0

# Optional: pywin32 is only for Windows (Word PDF export)
# Install separately on Windows: pip install pywin32>=312

# Development tools (optional, for code quality):
ruff>=0.9.0
pytest>=8.3
