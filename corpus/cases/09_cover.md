# 封面样例

本样例用于验证封面页渲染：封面由 config 的 `cover` 段驱动，标题、单位、日期自动注入，
并生成 CoverInfo 分节，使后续正文从新页开始。

## 一、封面要素

封面应包含文件标题、编制单位与日期，字体与间距遵循 reference_doc 的封面样式。

## 二、验证要点

验证三个 renderer 下封面页不溢出、不出现多余空白页（封面后空 CoverInfo 段是关键）。
