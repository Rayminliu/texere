# 页眉页脚样例

本样例用于验证页眉页脚在三个 renderer 下的一致性：页眉应显示左侧标题与右侧标识，
页脚居中显示页码（"第 N 页"）。页眉样式来自 `reference_doc` 的版式契约。

## 一、页眉继承

页眉内容由 config 的 `header` 段提供，正文中不重复书写。

## 二、页脚页码

页脚页码由 config 的 `footer` 段提供，渲染器负责逐页填充实际页码。

## 三、条件继承说明

部分模板页眉为条件继承：封面/目录页可能不显示页眉，正文页才显示，
需验证三个 renderer 对该条件的一致处理。
